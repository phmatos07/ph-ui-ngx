name: deployment
on: [push]
jobs:
  LINT-ALL:
    name: Analisar Código Estático
    runs-on: ubuntu-latest

    steps:
      - uses: actions/setup-node@v1

      - name: ESLint
        run: npm run lint

      - name: Lint/SASS
        run: npm run lint:sass
