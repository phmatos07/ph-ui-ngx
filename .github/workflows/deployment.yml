name: DEPLOYMENT
on: [push]
jobs:
  LINT-ALL:
    name: Análise de código estático (LINTER)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v1

      - name: Install
        run: npm install

      - name: ESLint
        run: npm run lint

      - name: Lint/SASS
        run: npm run lint:sass

  BUILD:
    name: Builder da biblioteca
    runs-on: ubuntu-latest
    steps:
      - name: Install
        run: npm run build:core
